#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${repository_root}/backend"
uv run --isolated --locked --group chroma-spike \
  python -m job_hunter.infrastructure.chroma
