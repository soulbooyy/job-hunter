#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${repository_root}/backend"
uv run --locked --no-sync python -m job_hunter.infrastructure.persistence.migrate
